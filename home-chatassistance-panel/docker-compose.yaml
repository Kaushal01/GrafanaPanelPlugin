version: '3'

services:
  grafana:
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
    depends_on:
      - backend

  backend:
    build: ../backend
    container_name: backend
    ports:
      - "5000:5000"